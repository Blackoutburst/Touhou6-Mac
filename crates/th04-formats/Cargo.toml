[package]
name = "th04-formats"
version = "0.1.0"
edition = "2021"
license = "MIT"

# Parsers for Touhou 4 (Lotus Land Story, PC-98) data formats.
# Pure parse, zero dependencies — mirrors the th06 `formats` crate.

[dependencies]

[[example]]
name = "unpack"
path = "examples/unpack.rs"
