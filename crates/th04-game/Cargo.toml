[package]
name = "th04-game"
version = "0.1.0"
edition = "2021"
license = "MIT"

# The TH04 game: decodes TH04 assets via th04-formats and renders them through
# the shared th06-engine (wgpu). Currently a thin harness that draws the title.

[dependencies]
th04-formats = { path = "../th04-formats" }
th06-engine = { path = "../engine" }
image = "0.25"
